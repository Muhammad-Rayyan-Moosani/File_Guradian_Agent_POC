flask
flask-cors
waitress
python-dotenv
watchdog
pandas
numpy
anthropic
openai
# openai also drives the OpenAI / Local / Vertex AI providers (via base_url).
# The database is local SQLite via Python's built-in sqlite3 — no package needed.
